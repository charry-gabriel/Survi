{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "jobs/lumberjack.yml",
  "description": "Configuration du Bûcheron (LUMBERJACK). Tableaux de 11 valeurs (index = niveau 0 à 10).",
  "type": "object",
  "required": [
    "drop-multiplier", "charcoal-chance", "apple-leaf-chance", "fire-damage-multiplier",
    "tree-feller-extra-logs", "fire-resistance-ticks",
    "sapling-growth-allow-chance", "sapling-extra-growth-chance",
    "sapling-third-tick-chance-at-max", "sapling-bone-meal-chance",
    "respawn-cooldown-seconds"
  ],
  "additionalProperties": false,
  "properties": {
    "drop-multiplier":                { "$ref": "#/definitions/non-negative-number-array", "description": "Multiplicateur de drops sur les bûches." },
    "charcoal-chance":                { "$ref": "#/definitions/probability-array",         "description": "Probabilité d'obtenir du charbon de bois bonus en cassant un log." },
    "apple-leaf-chance":              { "$ref": "#/definitions/probability-array",         "description": "Probabilité d'obtenir une pomme en cassant des feuilles de chêne/azalée." },
    "fire-damage-multiplier":         { "$ref": "#/definitions/non-negative-number-array", "description": "Multiplicateur des dégâts de feu reçus (1.0 = vanilla)." },
    "tree-feller-extra-logs":         { "$ref": "#/definitions/non-negative-int-array",    "description": "Logs supplémentaires abattus par le tree-feller (0 = désactivé)." },
    "fire-resistance-ticks":          { "$ref": "#/definitions/non-negative-int-array",    "description": "Durée en ticks de la résistance au feu lors d'une ignition (0 = aucune)." },
    "sapling-growth-allow-chance":    { "$ref": "#/definitions/probability-array",         "description": "Probabilité d'autoriser un tick de croissance sur un sapling planté (niv.0–4 = ralentissement)." },
    "sapling-extra-growth-chance":    { "$ref": "#/definitions/probability-array",         "description": "Probabilité d'un tick de croissance bonus après un tick normal (niv.6–10)." },
    "sapling-third-tick-chance-at-max": { "type": "number", "minimum": 0, "maximum": 1,   "description": "Probabilité d'un 3e tick bonus au niveau maximum." },
    "sapling-bone-meal-chance":       { "$ref": "#/definitions/probability-array",         "description": "Probabilité que la farine d'os fonctionne sur un sapling planté par un bûcheron." },
    "respawn-cooldown-seconds":       { "$ref": "#/definitions/non-negative-int-array",    "description": "Cooldown en secondes avant de réutiliser le spawn personnalisé (lit/ancre) après un respawn. Index = niveau Bûcheron." }
  },
  "definitions": {
    "non-negative-number-array": {
      "type": "array", "minItems": 11, "maxItems": 11,
      "items": { "type": "number", "minimum": 0 }
    },
    "probability-array": {
      "type": "array", "minItems": 11, "maxItems": 11,
      "items": { "type": "number", "minimum": 0, "maximum": 1 }
    },
    "non-negative-int-array": {
      "type": "array", "minItems": 11, "maxItems": 11,
      "items": { "type": "integer", "minimum": 0 }
    }
  }
}